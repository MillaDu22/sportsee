// Modèle pour les durees moyennes sessions d'activité de l'utilisateur //
const UserAverageSessionsModel = {
    sessions: [
        {
            day: Number,
            sessionLength: Number
        }
    ]
}
export { UserAverageSessionsModel };